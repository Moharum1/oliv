
fn main() {
    // let ray_origin = CoOrdinate::new_point(0.0, 0.0, -5.0);
    // let mut canvas = Canvas::new(50, 50);
    // let pixel_size = 50 * 50 / 10;
    // let world_z = 10.0;
    // let half: i32 = pixel_size / 2;
    //
    // let sphere = Sphere::new();
    // let red = Color::new(255.0, 0.0, 0.0);
    //
    // for y in 0..(50 * 50){
    //     let world_y = half - pixel_size * y;
    //
    //     for x in 0..(50 * 50){
    //
    //         let world_x = -half + pixel_size * x;
    //         let pos = CoOrdinate::new_point(world_x as f32, world_y as f32, world_z);
    //         let norm = (pos - ray_origin).normalize();
    //         let ray = Ray::new((0.0, 0.0, -5.0), (norm.x, norm.y, norm.z));
    //         let intersect = sphere.intersect(ray);
    //
    //         if let Some(_) = intersect{
    //             canvas.write_pixel(x as usize, y as usize, red);
    //         }
    //     }
    // }
    //
    // canvas.to_ppm("Sphere");

}
